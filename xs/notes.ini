project.name = notes

[id]
type = id

[title]
index = mixed

[content]
index = mixed

[hidden]
type = numeric
index = self
tokenizer = full

[book_hidden]
type = numeric
index = self
tokenizer = full

[deleted]
type = numeric
index = self
tokenizer = full

[book_deleted]
type = numeric
index = self
tokenizer = full
